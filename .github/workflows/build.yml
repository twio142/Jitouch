name: Build

on:
  workflow_dispatch:
  workflow_call:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build agent (Jitouch.app)
        run: |
          xcodebuild \
            -project jitouch/Jitouch/Jitouch.xcodeproj \
            -scheme Jitouch \
            -configuration Release \
            SYMROOT="$PWD/build/agent" \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            build

      - name: Stage Jitouch.app for prefpane
        run: cp -R build/agent/Release/Jitouch.app prefpane/

      - name: Build prefpane (Jitouch.prefPane)
        run: |
          xcodebuild \
            -project prefpane/Jitouch.xcodeproj \
            -target Jitouch \
            -configuration Release \
            SYMROOT="$PWD/build/prefpane" \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            build

      - name: Fix permissions and sign
        run: |
          chmod +x build/prefpane/Release/Jitouch.prefPane/Contents/MacOS/Jitouch
          chmod +x build/prefpane/Release/Jitouch.prefPane/Contents/Resources/Jitouch.app/Contents/MacOS/Jitouch
          codesign --sign - --force build/prefpane/Release/Jitouch.prefPane/Contents/Resources/Jitouch.app

      - name: Stage artifact
        run: |
          mkdir build/dist
          cp -R build/prefpane/Release/Jitouch.prefPane build/dist/

      - name: Upload Jitouch.prefPane
        uses: actions/upload-artifact@v4
        with:
          name: Jitouch.prefPane
          path: build/dist/
